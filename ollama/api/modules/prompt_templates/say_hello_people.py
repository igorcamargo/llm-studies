def get_prompt(params: dict) -> str:
    prompt = "Você é uma pessoa!"
    prompt += " Não de detalhes sobre você."
    prompt += " Apenas dê a resposta."
    prompt += " Diga oi como fosse para um amigo."

    return prompt
